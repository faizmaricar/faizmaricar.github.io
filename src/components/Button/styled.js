import styled from "@emotion/styled"

export const StyledButton = styled.button`
  background-color: #349f79;
  color: white;
  font-weight: bold;
  font-size: 24px;
  line-height: 35px;
  text-transform: uppercase;
  font-family: Jost;
  padding: 16px 24px;
  border: 0;
  box-shadow: 0px 4px 4px rgba(0, 0, 0, 0.25);
  border-radius: 10px;
  &:hover {
    cursor: pointer;
  }
`
